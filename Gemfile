source "https://rubygems.org"

# Use the github-pages gem for local parity with GitHub Pages
# This pins a compatible set of Jekyll and plugin versions that GitHub Pages uses.
gem "github-pages", group: :jekyll_plugins
# If you prefer to use specific plugins locally/in Actions (not supported by GitHub Pages native build),
# add them here, for example:
# gem "jekyll-archives"
